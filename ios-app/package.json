{
  "name": "spiritverse-ios-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:web": "cd .. && npm run export",
    "sync": "npx cap sync ios",
    "open": "npx cap open ios",
    "prepare": "npm run build:web && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/cli": "^5.0.4",
    "@capacitor/core": "^5.0.4",
    "@capacitor/ios": "^5.0.4"
  }
}
