# SpiritVerse Android app wrapper

This folder contains a Capacitor-based Android shell for the main Next.js app.

## Commands

From this folder:

```sh
npm install
npm run prepare
npm run open
```

## Notes
- The web production output is read from the parent project's `out/` folder.
- Use Android Studio with an emulator or a connected device to run the app.
